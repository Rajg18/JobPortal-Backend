# ─── Dev Profile ─────────────────────────────────────────────────────────────
# Activate with: --spring.profiles.active=dev
# These values are for LOCAL DEVELOPMENT ONLY. Never use in production.

spring.datasource.password=${DB_PASSWORD:626117}

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Dev-only JWT secret (test key — not used in production)
jwt.secret=${JWT_SECRET:VGhpcy1pcy1hLXZlcnktc2VjdXJlLWtleS1mb3Itand0}

cors.allowed-origins=http://localhost:3000,http://localhost:5173,http://localhost:8080
